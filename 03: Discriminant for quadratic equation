#Enter six floate number as 'a' to 'f', and use the comma to split the each number. 
a,b,c,d,e,f=[float(x) for x in input("Enter a,b,c,d,e,f:").split(",")]

#Definition the letter 'g' as Discriminant 
g= a * d - b * c

#If Discriminant(g) equals 0, we print the 'no solution' result
#If Discriminant(g) doesn't equal 0, we print the values of 'x' and 'y' in the quadratic equation
if g == 0:
    print("The equation has no solution.")
else:
    print( "x is ",( e * d - b * f) / g , 1 , "and y is ", (a * f - e * c ) / g )
