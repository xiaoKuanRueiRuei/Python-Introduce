#Create a float number as 'a' which value is the weight in pounds
a=float(input('Enter weight in pounds:'))

#Create two float numbers as 'b' and 'c' which values are the height in feet and inches
b=float(input('Enter feet:'))
c=float(input('Enter inches:'))

#Create a float number as 'd' to transfer the weigrt from pound to kilometer
d=a*0.45359327

#Create a float number as 'e' to transfer the heigrt from feets and inches to meter
e=(b*12+c)*0.0254 

#Calculate the bmi 
bmi= d/e**2

#To determin the BMI is which gap to the health status
print("BMI is " , format(bmi) )
if bmi < 18.5 :
    print("You are Underweight.")
elif bmi < 25 :
    print("You are Normal.")
elif bmi < 30 :
    print("You are Overweight.")
else :
    print("You are Obese.")
